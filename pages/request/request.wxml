<!--pages/request/request.wxml-->
<view wx:for="{{items}}" wx:key="item" class='wrap' style='margin-top:40rpx;'>
    <image src='{{item.image}}' mode='aspectFit' style='width:200rpx;height:200rpx;'></image>
    <view>{{item.name}}</view>
</view>
